import MdxLayout from '@/components/MdxLayout';

export const resources = {
  docs: [
    { url: "https://www.krakend.io/docs/enterprise/endpoints/dynamic-routing/", label: "Dynamic Routing" },
    { url: "https://www.krakend.io/docs/enterprise/security-policies/policy-language/", label: "Security Policies" }
  ]
};

## Dynamic routing
Dynamic routing extends the routing capabilities to add header, tokens, and query string processing to assemble the final upstream URL you want to reach. This use case demonstrates how to route to a specific URL, taking into account the value of the `X-Route` header.

The configuration introduces a variable `{input_headers.x-route}` that injects the value of the header. This kind of routing should always be accompanied by a [Security Policy](https://www.krakend.io/docs/enterprise/security-policies/policy-language/) that enforces the passing of the header to accept the request.

You can test this configuration with:

```bash
curl -iG -H 'X-Route: hello' 'http://localhost:8080/dynamic/header'
```

export default function MDXPage({ children }) {
  return <MdxLayout resources={resources}>{children}</MdxLayout>
}
