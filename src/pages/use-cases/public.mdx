import MdxLayout from '@/components/MdxLayout';

## About this demo
- One
- Two
- Three

## Endpoint
```
/public
```

A public endpoint with no authentication required.

[http://localhost:8080/public](http://localhost:8080/public)

Try it interactively in the [Playground SPA](http://localhost:3000#auth-demo/public-private).

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>
}
