import MdxLayout from '@/components/MdxLayout';

export const resources = {
  docs: [
    { url: "https://www.krakend.io/docs/enterprise/backends/grpc/", label: "gRPC to REST" }
  ]
};

## gRPC parameter mapping

![gRPC to REST](https://www.krakend.io/images/documentation/grpc.dark.png)
The `/travel_book_flight/{id_flight}/{main_passenger}` shows how to map parameters coming in a request to gRPC payload.

This use case is similar to the [`/travel` endpoint](/demo/use-cases/travel).

export default function MDXPage({ children }) {
  return <MdxLayout resources={resources}>{children}</MdxLayout>
}
