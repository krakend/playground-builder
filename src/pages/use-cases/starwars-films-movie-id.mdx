import MdxLayout from '@/components/MdxLayout';

export const resources = {
  tryUrl: {
    endpoint: "http://localhost:8080/starwars_films/",
    placeholder: "movie id (1-6)"
  },
  docs: [
    { url: "https://www.krakend.io/docs/enterprise/backends/graphql/", label: "GraphQL backends" }
  ]
};

## GraphQL to REST
This use case demonstrates how to transform a GraphQL query into a regular REST API endpoint without the end-user needing to specify the query.

The query is written in the configuration so consumers do not know about the technology needed to get the data. The query can live in an external file if needed.

export default function MDXPage({ children }) {
  return <MdxLayout resources={resources}>{children}</MdxLayout>
}
