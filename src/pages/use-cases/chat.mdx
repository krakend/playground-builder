import MdxLayout from '@/components/MdxLayout';

## Endpoint
```
/chat
```

## Chat room using WebSockets
The endpoint `/chat` is an example endpoint where your web interface would connect to offer a WebSockets integration.

See the [WebSockets documentation](https://www.krakend.io/docs/enterprise/websockets/).

Open the interface: [http://localhost:8080/chat](http://localhost:8080/chat)


export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>
}

